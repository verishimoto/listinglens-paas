import 'package:flutter/material.dart';

class BetaTimeStream extends StatelessWidget {
  const BetaTimeStream({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Time Stream..."));
  }
}
